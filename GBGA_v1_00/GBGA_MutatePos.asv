function y=GBGA_MutatePos(x,mu)

    goodMutate = false;
    
    while (~goodMutate)
        nVar=numel(x);

        nmu=ceil(mu*nVar);

        j=randsample(nVar,nmu);

        y=x;
        y(j)=1-x(j);
        
        ones_x = numel(find(x));
        ones_
        
    end
end